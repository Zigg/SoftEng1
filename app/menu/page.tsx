// TODO: This will obviously be within the main dashboard page and rendered as a component or might serve as its own page idk yet

const MenuPage = () => {
  return <div>Menu Page</div>;
};

export default MenuPage;
